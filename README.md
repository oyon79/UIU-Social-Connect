
  # UIU Social Connect Prototype

  This is a code bundle for UIU Social Connect Prototype. The original project is available at https://www.figma.com/design/BFMdwIGV66XAveUlScs70K/UIU-Social-Connect-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  